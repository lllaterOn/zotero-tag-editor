# v1.0.0 验收记录

维护者已在本地试用阶段确认功能完整，并于 2026-09-25 授权以现有功能正式发布 v1.0.0、采用 MIT、启用公开自动更新。此次不再增加功能。

自动验证包括 TypeScript、16 项领域与运行时测试、清单字段、仓库卫生、XPI 内容、发布资产和跨时区可重复构建。浏览器检查使用实际 XHTML 与模拟桥接，覆盖增删改、草稿、冲突、目标切换、快捷键和布局；这些检查不等于原生验收。

原生验证使用 Windows 上的 Zotero 10.0.3，独立配置及测试数据库，检查正式资产的加载、菜单、Ctrl+T、原有标签显示、快捷键录入、重新选择后在同一窗口切换、取消切换保留草稿、接受切换清除草稿、保存及再次打开撤销。用户日常文献库不参与测试。

正式发布执行顺序：本地验证 → GitHub CI 构建 Draft → 下载 Draft 原始 XPI 与 SHA256SUMS → 校验包和发布标签源码 → 隔离原生检查 → 发布同一 Draft → 自动更新清单 → 再次核对正式资产。发布标签对应的 GitHub Actions 和 Release 资产提供可追溯证据，不把尚未完成的步骤描述为已通过。

维护者的试用反馈覆盖 Windows 实际界面、多选标签、菜单、快捷键、窗口大小、浮层与切换选择；未据此声称完整覆盖所有边界。其他操作系统、其他 Zotero 版本及所有 PDF 阅读器组合未完整验收；兼容声明范围不等同于全范围测试。

## 后续发布检查

- 下载 Draft 资产，SHA-256 与 SHA256SUMS 一致，包内版本、插件 ID、更新地址与标签源码一致。
- 在隔离 Zotero 中验证安装、重启、启停、文献列表和 PDF 父条目入口；无效或只读目标不写入。
- 验证多选添加、移除、改名合并，保留未操作标签及自动标签类型，确认后台选择变化不改变旧草稿保存对象。
- 验证保存与撤销的外部冲突保护、事务回滚、再次呼出的目标切换及未保存确认。
- 检查明暗主题、窄窗口、长标签、输入法、候选浮层与自定义快捷键。
- 发布原始 Draft 资产，检查 publish-update 成功，正式资产摘要与更新清单一致。
